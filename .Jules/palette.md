## 2024-05-02 - Keyboard Accessibility Initiative\n**Learning:** The application heavily relies on interactive buttons with only icons inside them, none of which have ARIA labels. This makes it impossible for screen reader users to understand their purpose. Furthermore, elements like `ThemeToggle` lack accessible names.\n**Action:** Started adding `aria-label` to icon-only buttons to improve accessibility across the application.
